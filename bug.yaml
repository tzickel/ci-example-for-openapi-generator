swagger: "2.0"
info:
  title: "test"
host: "localhost:8080"
paths:
  /GetBackBinaryData:
    get:
      operationId: "GetBackBinaryData"
      produces:
      - "application/octet-stream"
      responses:
        "200":
          description: "successful operation"
          schema:
            type: string
            format: binary
